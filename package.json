{
  "name": "colisee-injector",
  "description": "Micro-service that manually and safely schedules games on the SIG-Game Colisee.",
  "version": "0.0.1",
  "main": "src/index.js",
  "scripts": {
    "quick-start": "node src/index.js",
    "prestart": "npm run build",
    "start": "npm run quick-start",
    "build": "npm run build:src && npm run build:client",
    "build:src": "tsc --project tsconfig-src.json",
    "build:client": "npm run build:client:tsc && npm run build:client:browserify && npm run build:client:uglify",
    "build:client:tsc": "tsc --project tsconfig-client.json",
    "build:client:browserify": "browserify client/index.js > client/bundle.js",
    "build:client:uglify": "uglifyjs client/bundle.js > client/bundle.min.js",
    "presetup": "npm install",
    "setup": "bower install",
    "predocs": "npm run build",
    "docs": "npm run quick-docs",
    "quick-docs": "npm run quick-docs:ts && npm run quick-docs:api && npm run quick-docs:readme",
    "quick-docs:api": "apidoc --input src/ --output docs/api/",
    "quick-docs:ts": "typedoc --out docs/ts/",
    "quick-docs:readme": "cp README.md docs/",
    "quick-test": "mocha --colors test/",
    "pretest": "npm run build",
    "test": "npm run quick-test",
    "clean": "npm run clean:src && npm run clean:test && npm run clean:npm && npm run clean:bower",
    "clean:src": "rm -f src/*.js && rm -f src/*.js.map && rm -f src/**/*.js && rm -f src/**/*.js.map",
    "clean:test": "rm -f test/*.js && rm -f test/*.js.map && rm -f test/**/*.js && rm -f test/**/*.js.map",
    "clean:npm": "rm -rf node_modules/ npm-debug.log",
    "clean:bower": "rm -rf bower_components/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/siggame/colisee-injector.git"
  },
  "author": "siggame",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/siggame/colisee-injector/issues"
  },
  "homepage": "https://github.com/siggame/colisee-injector#readme",
  "devDependencies": {
    "@types/body-parser": "0.0.34",
    "@types/chai": "^3.4.34",
    "@types/cors": "^2.8.0",
    "@types/dotenv": "^2.0.20",
    "@types/express": "^4.0.35",
    "@types/knex": "0.0.41",
    "@types/lodash": "^4.14.52",
    "@types/mocha": "^2.2.39",
    "@types/node": "^7.0.5",
    "@types/react": "^15.0.13",
    "@types/react-dom": "^0.14.23",
    "apidoc": "^0.17.5",
    "bower": "^1.8.0",
    "browserify": "^14.1.0",
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "typedoc": "^0.5.7"
  },
  "dependencies": {
    "apidoc": "^0.17.5",
    "body-parser": "^1.16.1",
    "cors": "^2.8.1",
    "dotenv": "^4.0.0",
    "express": "^4.14.1",
    "knex": "^0.12.7",
    "lodash": "^4.17.4",
    "pg": "^6.1.2",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "typescript": "^2.1.5"
  }
}
